// Empty interface denoting that an object is used by object pooling.
// Make sure that any object being used by the object pooler implements this in its main logic script.
// It finds the index through it.
public interface IPoolable {

}